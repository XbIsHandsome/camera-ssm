<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/beans  
                        http://www.springframework.org/schema/beans/spring-beans-3.1.xsd  
                        http://www.springframework.org/schema/context  
                        http://www.springframework.org/schema/context/spring-context-3.1.xsd  
                        http://www.springframework.org/schema/mvc  
                        http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd">

	<mvc:annotation-driven />

	<!-- 自动扫描该包，使SpringMVC认为包下用了@controller注解的类是控制器 -->
	<context:component-scan base-package="com.camera" />
	<import resource="classpath:spring-mybatis.xml"/>
	<!--
	<context:component-scan base-package="com.camera.dao" />
	<context:component-scan base-package="com.camera.service" />
	<context:component-scan base-package="com.camera.util" />
	-->
	
	
	<!-- 定时发送命令 -->
	<bean id="SendCommandTask" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean" lazy-init="true">
		<!-- 处理定时任务的类 -->
		<property name="targetObject" >
			<bean class="com.camera.util.SendCommand"/>
		</property>
		<!-- 定时任务执行的方法 -->
		<property name="targetMethod" value="testQuartz"/>
		<!--作业不并发调度-->
		<property name="concurrent" value="false"/>
	</bean>

	<bean id="receivedCaseCountJobTrigger" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean" lazy-init="true">
		<property name="jobDetail" ref="SendCommandTask" />
		<!-- 指定Cron表达式：周一到周五7点、12点执行调度  -->
		<!-- <property name="cronExpression" value="0 0 8,10,14,16,19 * * ? *" />  -->
		<property name="cronExpression" value="0/10 * * * * ? "></property>
	</bean>

	<bean class="org.springframework.scheduling.quartz.SchedulerFactoryBean" lazy-init="true">
		<property name="triggers">
			<list>
				<ref bean="receivedCaseCountJobTrigger" />
			</list>
		</property>
	</bean>
</beans>